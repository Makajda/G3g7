<table class="tableFixHead">
    <thead>
        <tr>
            <NextView World="World" Octave="Octave" NextChanged="@(() => StateHasChanged())" />
        </tr>
    </thead>
    <tr>
        <DoView World="World" Octave="Octave" />
    </tr>
    <TiLaSoView World="World" Octave="Octave" />
    <FaMiReView World="World" Octave="Octave" />
</table>

@code {
    [Parameter] public World World { get; set; }
    [Parameter] public Octave Octave { get; set; }
    [CascadingParameter] public bool IsShowValue { get; set; }
    [CascadingParameter] public string TitleChrom { get; set; }
}
