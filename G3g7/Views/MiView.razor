@inherits NoteView

<td class="title" style="@TitleChrom">@title</td>
@if (IsShowValue) {
    <td class="title" style="@TitleChrom">@Octave.Mi</td>
}

@if (World.IsNext) {
    <DoView World="World" Octave="Octave.NextTwo" />
}

@code {
    private string title = "Mi";
}
