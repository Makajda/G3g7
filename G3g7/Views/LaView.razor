@inherits NoteView

<td class="title" style=@(TitleChrom is null ? null : "background-color: yellow;" + TitleChrom)>@title</td>
@if (IsShowValue) {
    <td class="title" style="@TitleChrom">@Octave.La</td>
}

@code {
    private string title = "La";
}
