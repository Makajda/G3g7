@inherits NoteView

@if (Octave.IsVisible) {
    <tr>
        @{ AddTd(__builder); }
        <TiView World="World" Octave="Octave" />
    </tr>
    <tr>
        @{ AddTd(__builder); }
        <LaView World="World" Octave="Octave" />
    </tr>
}

@code {
}
